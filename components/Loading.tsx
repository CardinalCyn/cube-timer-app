import { View } from "react-native";
import { TextCustomFont } from "./TextCustomFont";

export default function Loading() {
  return (
    <View>
      <TextCustomFont>Loading currently</TextCustomFont>
    </View>
  );
}
